package com.pickaid.pmmojs.contents.perks;

import net.minecraft.resources.ResourceLocation;
import net.minecraft.world.entity.player.Player;

public class PerkStopContextJS extends AbstractPerkContextJS {
    public PerkStopContextJS(ResourceLocation perkId, Player player, PerkTagJS settings) {
        super(perkId, player, settings);
    }
}
